# S2 — Sorting (`sort_by`)

Add `query(sort_by=field)`: sort results ASCENDING by that field's value.

POLICY (decided once here): records with EQUAL sort keys keep their INSERTION ORDER
(i.e., a stable sort).

Implement only this session's feature.
