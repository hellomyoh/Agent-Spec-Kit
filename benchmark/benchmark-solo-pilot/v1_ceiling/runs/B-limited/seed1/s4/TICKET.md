# S4 — Projection (`select`)

Add `query(select=[field, ...])`: each returned record contains ONLY the selected keys
(those present on the record). All other query behaviour is unchanged.

Implement only this session's feature.
