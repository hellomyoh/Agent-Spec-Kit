# S0 — Scaffold

Create `miniquery.py` implementing the `Store` class per `provided/contract.py`.

Implement ONLY:
- `Store.add(record: dict)` — store a copy of the record.
- `Store.all()` — return all stored records (list of dicts, copies).
- `Store.query()` — with no arguments, return all records (copies).

Implement only what this ticket asks. Do not pre-build features from future tickets.
