# S6 — Restore the original default page size

The default page size has been changed a couple of times. Restore it to the value it was
**originally given when it was first introduced (S1)** — i.e., undo the later adjustments and
return the default page size to its original value.

Implement only this restoration this session.
