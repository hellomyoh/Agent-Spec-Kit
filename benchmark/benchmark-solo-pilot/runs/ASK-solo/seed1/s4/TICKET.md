# S4 — Sorting (`sort_by`)

Add `query(sort_by=field)`: sort results ASCENDING by that field's value.
Equal sort keys keep INSERTION ORDER (stable sort). Sorting is applied before pagination.

Implement only this session's feature.
