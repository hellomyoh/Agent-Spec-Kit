# Vibe ASK-Solo Benchmark Plan

이 문서는 Agent-Spec-Kit의 핵심 가설을 검증하기 위한 벤치마크 계획서이자, Codex / Claude 같은 코딩 에이전트에 그대로 붙여 넣어 사용할 수 있는 실행 프롬프트다.

## 1. 검증 목표

검증할 핵심 가설:

> 바이브 코딩 상황에서 사용자는 요구사항을 완전하고 일관되게 지시하지 못하며, 과거에 어떤 의도와 제약을 넣었는지도 자주 잊는다. ASK-solo는 기능명세서와 SSOT 문서를 통해 이 빈약한 지시를 보완하고, 목표 달성률과 결과 품질을 높이며, drift와 재작업을 줄인다.

이 벤치마크는 단순히 “에이전트가 과거 결정을 기억하는가”를 보지 않는다. 다음 네 가지를 함께 본다.

1. 코드 테스트: 최종 구현이 요구 기능과 hidden invariant를 만족하는가
2. 문서 품질: 기능명세서 / SSOT가 실제 작업에 도움이 되는 구조와 내용을 갖췄는가
3. 대화 과정: 에이전트가 모호한 지시를 적절히 해석하고, 필요할 때 질문하고, 충돌을 감지했는가
4. 비용: 목표 달성까지 소모된 토큰, 시간, 도구 호출, 대화 왕복 수가 어느 정도인가

## 2. 비교 설계

### 2.1 비교 축

| 축 | 값 |
|---|---|
| 사용자 프롬프트 수준 | 초급 / 중급 / 고급 |
| 작업 방식 | 일반 진행 / ASK-solo |
| 과제 유형 | 동일 앱 또는 동일 기능군 |
| 반복 | seed 3개 이상 권장 |

최소 pilot:

```text
프롬프트 수준 3개 × 작업 방식 2개 × seed 3개 = 18 runs
```

처음에는 seed 1개로 dry-run을 먼저 수행해 하네스와 채점 기준을 검증한다.

### 2.2 작업 방식 정의

**일반 진행 baseline**

- 에이전트는 현재 사용자 요청과 현재 코드만 보고 작업한다.
- 별도 기능명세서 / SSOT 구조를 만들도록 강제하지 않는다.
- 필요한 경우 짧은 notes 정도는 남길 수 있지만, 구조화된 권위 문서로 취급하지 않는다.

**ASK-solo**

- 에이전트는 작업 전 기능명세서와 SSOT 문서를 만들거나 갱신한다.
- 코드 구현 전후로 기능명세서 / ARCHITECTURE / DECISIONS / PROGRESS / QA 기준을 확인한다.
- 사용자의 최신 지시가 기존 SSOT와 충돌하면 바로 구현하지 않고 다음 중 하나를 선택한다.
  - 기존 명세 유지
  - 사용자에게 확인 질문
  - 의도적 변경으로 판단하고 SSOT / 기능명세 / ADR 성격의 결정 문서를 먼저 갱신
  - 요청을 보류하고 충돌 사유 기록

## 3. 사용자 프롬프트 수준 정의

프롬프트 수준은 결과를 보고 재분류하지 않는다. 실험 전에 고정한다.

### 3.1 초급 프롬프트

특징:

- 목적은 있지만 기능 범위와 예외 조건이 빈약하다.
- “알아서”, “자연스럽게”, “편하게”, “전처럼” 같은 표현이 많다.
- 권한, 데이터, 실패 조건, 테스트 기준이 거의 없다.

예:

```text
검색 기능 좀 편하게 만들어줘. 이상한 값이 들어와도 너무 에러 안 나게 해줘.
```

### 3.2 중급 프롬프트

특징:

- 주요 기능과 일부 제약은 말한다.
- 예외, 권한, 데이터 계약, 회귀 조건 일부가 빠져 있다.
- 구현 방향은 어느 정도 있으나 제품 의도와 충돌할 수 있다.

예:

```text
검색에서 없는 필드는 빈 결과로 처리하고, 정렬과 페이지네이션은 기존처럼 유지해줘. 기존 테스트도 깨지면 안 돼.
```

### 3.3 고급 프롬프트

특징:

- 기능, 제약, 예외, 권한, 데이터 계약, 테스트 기준이 비교적 명확하다.
- 구현자가 물어봐야 할 부분이 적다.
- ASK의 추가 이득이 작을 수 있다.

예:

```text
where 필드가 전체 레코드에 존재하지 않으면 KeyError를 유지한다. 단, select에서 없는 필드는 레코드별로 생략한다. 정렬은 stable sort를 유지하고, 기존 where/sort/limit/select 회귀 테스트를 모두 통과해야 한다.
```

## 4. 과제 설계 원칙

좋은 과제는 “에이전트가 현재 코드만 읽으면 쉽게 맞히는 문제”가 아니다. 다음 조건을 포함해야 한다.

1. 초반에 제품 목표 / 금지사항 / 정책을 한 번 도입한다.
2. 중반에 기능 추가와 예외 처리를 넣는다.
3. 후반에 사용자가 모호하거나 충돌하는 요청을 한다.
4. 최신 요청만 따르면 실패하고, SSOT / 기능명세를 확인하면 올바르게 처리할 수 있어야 한다.
5. 일부 요청은 진짜 방향 변경이어야 하고, 일부는 사용자 망각 또는 모호한 drift여야 한다.

예시 세션 구조:

| 세션 | 목적 | 사용자 지시 성격 |
|---|---|---|
| S0 | 초기 앱/라이브러리 골격 | 비교적 명확 |
| S1 | 핵심 정책 도입 | 한 번만 명시 |
| S2 | 일반 기능 추가 | 명확 |
| S3 | 횡단 관심사 추가 | 중간 수준 |
| S4 | 모호한 UX/예외 요청 | 초급형 |
| S5 | 기존 정책과 충돌 가능한 요청 | 초급/중급형 |
| S6 | “전처럼”, “자연스럽게” 같은 복원/변경 요청 | 의도 판단 필요 |

## 5. 측정 항목

### 5.1 코드 테스트 지표

| 지표 | 설명 |
|---|---|
| functional pass rate | 공개 기능 테스트 통과율 |
| hidden invariant violations | 초반 정책 / 보안 / 데이터 계약 위반 수 |
| regression count | 이전 세션에서 통과했던 동작이 깨진 수 |
| cross-layer completeness | UI/API/DB/cache/docs 경로가 일관되게 연결됐는지 |
| chain-death | import 실패, 앱 실행 실패, 핵심 API 누락 |

### 5.2 문서 품질 지표

| 지표 | 설명 |
|---|---|
| SSOT completeness | 목표, 기능, 데이터, 예외, 결정, 진행 상태가 필요한 만큼 기록됐는가 |
| authority clarity | 최신 채팅 지시, feature, architecture, decision의 권위 관계가 명확한가 |
| update correctness | 코드 변경에 맞춰 문서가 갱신됐는가 |
| no fake certainty | 확인하지 않은 내용을 확정 사실처럼 쓰지 않았는가 |
| reuse value | 다음 세션 에이전트가 실제로 읽고 활용할 수 있는가 |

### 5.3 대화 과정 지표

| 지표 | 설명 |
|---|---|
| clarification quality | 물어봐야 할 때 핵심 질문을 했는가 |
| over-questioning | 물어보지 않아도 될 것을 과도하게 물었는가 |
| conflict detection | 최신 요청과 기존 명세의 충돌을 감지했는가 |
| change classification | 의도적 변경 / 사용자 망각 / 모호성을 구분했는가 |
| user burden | 목표 달성까지 필요한 사용자 답변 수 |

### 5.4 프롬프트 통계 지표

| 지표 | 설명 |
|---|---|
| prompt token count | 사용자 프롬프트 길이 |
| technical term density | 개발 전문용어 비율 |
| requirement explicitness | 기능/제약/예외/테스트 기준 명시 정도 |
| ambiguity index | “알아서”, “적당히”, “전처럼” 같은 모호 표현 수 |
| conflict signal count | 과거 명세와 충돌 가능성이 있는 표현 수 |
| revision count | 중간 요구 변경 / 번복 횟수 |

### 5.5 비용 지표

| 지표 | 설명 |
|---|---|
| total tokens | 전체 토큰 |
| wall time | 작업 소요 시간 |
| tool calls | 도구 호출 수 |
| edit churn | 변경 파일 수 / diff 크기 |
| rework count | 실패 후 재수정 횟수 |
| conversation turns | 사용자-에이전트 왕복 수 |

## 6. 채점 방식

최종 점수는 코드 결과를 중심으로 하되, 문서와 과정도 별도 축으로 본다.

권장 가중치:

| 영역 | 가중치 |
|---|---:|
| 코드 테스트 / hidden invariant | 45 |
| 목표 달성 / 제품 의도 유지 | 20 |
| 문서 품질 / SSOT 활용성 | 15 |
| 대화 과정 / 질문 품질 | 10 |
| 비용 효율 | 10 |

주의:

- 로그나 문서가 그럴듯하다는 이유만으로 높은 점수를 주지 않는다.
- 문서 점수는 “다음 세션에서 실제로 유용한가”를 기준으로 한다.
- ASK가 질문을 했다는 이유만으로 감점하지 않는다. 필요한 질문이면 과정 품질 점수를 준다.
- 반대로 질문 없이 구현했더라도 기존 SSOT와 충돌했다면 감점한다.

## 7. 로그 수집 형식

각 run은 다음 파일을 남긴다.

```text
runs/<mode>/<level>/<seed>/
  prompts/
    s00_user_prompt.md
    s01_user_prompt.md
    ...
  conversation/
    s00_log.md
    s01_log.md
    ...
  work/
    <최종 코드>
  docs/
    <ASK-solo 문서 또는 baseline notes>
  scores/
    code_scores.jsonl
    doc_scores.jsonl
    process_scores.jsonl
    cost.jsonl
  final_report.md
```

conversation log에는 다음을 포함한다.

- 사용자 프롬프트 원문
- 에이전트의 확인 질문
- 사용자 답변
- 에이전트의 구현 계획
- SSOT / 기능명세 갱신 내역
- 실행한 테스트 명령과 결과
- 최종 요약

## 8. 분석 질문

결과 분석은 다음 질문에 답해야 한다.

1. 초급 프롬프트에서 ASK-solo가 일반 진행보다 목표 달성률을 높였는가
2. 중급 프롬프트에서 ASK-solo의 이득은 얼마나 남는가
3. 고급 프롬프트에서는 ASK 비용이 이득을 상쇄하는가
4. 모호성 지수가 높을수록 일반 진행의 drift가 증가하는가
5. ASK-solo는 모호성 지수가 높을 때 drift 증가를 완화하는가
6. ASK-solo가 질문을 늘렸다면, 그 질문이 재작업과 regression을 줄였는가
7. SSOT 문서 품질이 높을수록 후반 세션의 invariant violation이 줄어드는가

## 9. Go / No-Go 기준

Pilot 후 판단:

- **Go**: 초급/중급 프롬프트에서 ASK-solo가 일반 진행보다 hidden invariant / drift / rework를 줄이고, 비용 증가가 과도하지 않다.
- **Revise prompts**: 모든 방식이 쉽게 성공하면 프롬프트가 너무 명확하거나 과제가 너무 쉽다.
- **Revise task**: 모든 방식이 실패하면 과제 난이도나 scaffold가 과하다.
- **Revise ASK docs**: ASK-solo가 문서는 만들지만 후반 세션에서 활용하지 못하면 문서 구조를 줄이거나 권위 관계를 명확히 한다.
- **Stop or narrow claim**: 고급 프롬프트에서만 차이가 없고 초급/중급에서 이득이 있으면, ASK의 가치를 “초급/중급 사용자 보정”으로 한정한다.

## 10. Codex / Claude 실행 프롬프트

아래 프롬프트를 Codex 또는 Claude에 붙여 넣어 벤치마크를 진행한다.

```text
이 저장소에서 Vibe ASK-Solo Benchmark를 설계하고 실행 가능한 파일 구조로 구현하세요.

반드시 먼저 루트의 BENCHMARK-PLAN-VIBE-ASK-SOLO.md를 읽고, 그 계획을 기준으로 작업하세요.

목표:
- Agent-Spec-Kit의 ASK-solo가 초급/중급/고급 사용자 프롬프트에서 일반 진행 baseline보다 목표 달성률, 코드 품질, drift 억제, 재작업 감소에 도움이 되는지 측정한다.
- 코드 테스트 + 문서 품질 + 대화 과정 + 비용을 함께 기록하고 평가한다.

1차 구현 범위:
1. benchmark-vibe-ask-solo/ 폴더를 만든다.
2. 초급/중급/고급 프롬프트 세트를 각각 최소 1개 과제 × 3세션 이상으로 작성한다.
3. 비교 모드는 baseline-general 과 ask-solo 두 개로 둔다.
4. 각 run이 다음 로그를 남기도록 한다.
   - 사용자 프롬프트
   - 대화 로그
   - 코드 diff 또는 최종 코드
   - ASK 문서 또는 baseline notes
   - 테스트 결과
   - 문서 품질 점수
   - 과정 품질 점수
   - 비용 지표를 기록할 JSONL 골격
5. hidden test를 포함한 평가 하네스를 만든다.
6. 테스트 실패 메시지에서 정답 oracle이 누수되지 않게 한다.
7. pilot은 seed1만 실행 가능하게 만들고, seed 확장은 구조만 준비한다.

중요한 실험 원칙:
- 초급 프롬프트는 일부러 빈약하고 모호해야 한다.
- 중급 프롬프트는 일부 제약을 말하지만 예외와 회귀 기준이 빠져 있어야 한다.
- 고급 프롬프트는 기능, 예외, 테스트 기준이 비교적 명확해야 한다.
- 최신 사용자 지시가 과거 명세와 충돌하는 상황을 최소 1개 포함한다.
- 일부 충돌은 진짜 방향 변경이고, 일부는 사용자 망각 또는 모호한 drift여야 한다.
- ASK-solo는 기존 명세와 충돌을 감지하면 바로 구현하지 말고 확인/보류/명세 갱신/구현 중 올바른 행동을 선택해야 한다.
- baseline-general은 구조화 SSOT 없이 진행한다.

산출물:
- benchmark-vibe-ask-solo/README.md
- benchmark-vibe-ask-solo/driver.py 또는 동등한 실행 스크립트
- benchmark-vibe-ask-solo/tasks/
- benchmark-vibe-ask-solo/eval/
- benchmark-vibe-ask-solo/runs/ 구조 설명
- benchmark-vibe-ask-solo/PREREGISTRATION.md
- benchmark-vibe-ask-solo/RESULTS_TEMPLATE.md

완료 전에 다음을 보고하세요.
- 생성한 파일 목록
- 실험 설계 요약
- 초급/중급/고급 프롬프트 구분 기준
- baseline-general과 ask-solo의 차이
- 채점 지표
- 아직 실행하지 않은 부분과 다음 실행 명령
```

## 11. 프롬프트 사용법

1. 이 파일을 루트에 둔다.
2. 새 Codex / Claude 세션을 연다.
3. 위 `10. Codex / Claude 실행 프롬프트` 블록을 그대로 붙여 넣는다.
4. 에이전트가 `BENCHMARK-PLAN-VIBE-ASK-SOLO.md`를 먼저 읽게 한다.
5. 첫 실행은 전체 통계 실험이 아니라 `seed1` dry-run으로 한다.
6. dry-run에서 하네스와 채점 기준이 정상 작동하면 seed와 과제 수를 늘린다.

## 12. 해석 원칙

이 벤치마크가 증명할 수 있는 것:

- ASK-solo가 빈약하거나 모호한 사용자 지시에서 일반 진행보다 유리한지
- ASK-solo가 사용자의 의도 drift를 줄이는지
- ASK-solo의 문서 유지 비용이 결과 품질로 상쇄되는지
- 어떤 사용자 수준에서 ASK가 가장 효과적인지

이 벤치마크가 단독으로 증명하지 못하는 것:

- 모든 코딩 과제에서 ASK가 우월하다는 주장
- Team / Multi-Agent 방식의 효과
- 특정 모델 전체의 일반 성능
- 완벽한 제품 품질 보장

이번 계획은 ASK-solo의 적용 가치 범위를 좁고 검증 가능하게 밝히기 위한 것이다.
