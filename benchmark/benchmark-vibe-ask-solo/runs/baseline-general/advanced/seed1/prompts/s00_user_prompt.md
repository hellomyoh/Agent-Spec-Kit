# advanced S0 - user prompt

`Catalog` 클래스를 구현해줘: `add(item: dict)`, `all() -> list`, `search(query=None, where=None, sort_by=None, limit=None)`. 이번 세션 범위: `add`, `all`, 그리고 인자 없는 `search()`는 전체 아이템(복사본)을 반환.
